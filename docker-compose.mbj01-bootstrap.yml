# MBJ-01 fresh authorized bootstrap overlay.
#
# This overlay intentionally disables the broad browser-smoke seed and creates
# only the first synthetic administrator through the documented startup seed.
# The administrator password is injected at runtime and must never be committed.
# After initial bootstrap the app-side seed password is cleared while the
# acceptance probe retains its private runtime copy to prove persisted login.

services:
  app:
    environment:
      COGLATAS_BROWSER_SMOKE_SEED_ENABLED: "false"
      COGLATAS_BROWSER_SMOKE_RESPONSE_GATE_ENABLED: "false"
      COGLATAS_SEED_ADMIN_ENABLED: ${COGLATAS_MBJ01_SEED_ENABLED:-true}
      COGLATAS_SEED_ADMIN_EMAIL: ${COGLATAS_MBJ01_BOOTSTRAP_EMAIL:?set COGLATAS_MBJ01_BOOTSTRAP_EMAIL}
      COGLATAS_SEED_ADMIN_USERNAME: ${COGLATAS_MBJ01_BOOTSTRAP_DISPLAY_NAME:-MBJ01 Bootstrap Admin}
      COGLATAS_SEED_ADMIN_PASSWORD: ${COGLATAS_MBJ01_APP_SEED_PASSWORD:-}

  real-backend-playwright:
    environment:
      COGLATAS_MBJ01_BOOTSTRAP_EMAIL: ${COGLATAS_MBJ01_BOOTSTRAP_EMAIL:?set COGLATAS_MBJ01_BOOTSTRAP_EMAIL}
      COGLATAS_MBJ01_BOOTSTRAP_DISPLAY_NAME: ${COGLATAS_MBJ01_BOOTSTRAP_DISPLAY_NAME:-MBJ01 Bootstrap Admin}
      COGLATAS_MBJ01_BOOTSTRAP_PASSWORD: ${COGLATAS_MBJ01_BOOTSTRAP_PASSWORD:?set COGLATAS_MBJ01_BOOTSTRAP_PASSWORD}
